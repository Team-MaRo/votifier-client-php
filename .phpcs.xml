<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         name="PHP_CodeSniffer"
         xsi:noNamespaceSchemaLocation="vendor/squizlabs/php_codesniffer/phpcs.xsd">
    <description>A library in PHP to connect to a Minecraft server's Votifier plugin.</description>

    <config name="installed_paths" value="vendor/phpcompatibility/php-compatibility" />

    <file>src/</file>
    <file>tests/</file>

    <arg name="cache" value=".php_codesniffer_cache.json" />
    <arg name="colors" />
    <arg name="extensions" value="php" />

    <!--
        PHP-CS-Fixer (.php-cs-fixer.dist.php, the iwf-web/php-coding-standard set) owns
        code style. PHP_CodeSniffer is used only as the PHP-version-compatibility gate
        (PHPCompatibility, testVersion 7.1-); also enforcing PSR12 here just fought
        PHP-CS-Fixer's choices (concat spacing, single-line empty bodies, ...).
    -->
    <rule ref="PHPCompatibility" />
    <config name="testVersion" value="7.1-" />
</ruleset>
